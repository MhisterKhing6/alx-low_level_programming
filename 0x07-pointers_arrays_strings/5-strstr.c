#include <stdlib.h>
#include <stdio.h>
#include <string.h>
/**
 * _strstr - files first n values of a buffer with char
 * @s: A buffer to fill
 * @b: The value to fill the s
 * Return: the value of  s
 */

void print_chessboard(char (*a)[8])
{

}
int main()
{
int a[20][30];
int k = sizeof(a) / sizeof(a[1]);
printf("%d" ,k);
return 0;
}
