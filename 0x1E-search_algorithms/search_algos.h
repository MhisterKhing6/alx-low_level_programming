#ifndef SEARCH_ALGOS
#include "stdio.h"
#define SEARCH_ALGOS
int linear_search(int *, size_t, int);
void print_ar(int  *, size_t, size_t );
int bin(int *, size_t, size_t, int);
int binary_search(int *, size_t, int);

#endif