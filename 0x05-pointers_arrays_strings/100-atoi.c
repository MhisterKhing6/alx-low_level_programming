#include <stdio.h>
#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _atoi - Convert strings to integers
 * @src: a holder to copy string from
 * Return: returns an integer
 */
int _atoi(char *src)
{
return (atoi(src));
}
