#include <stdio.h>
#include "main.h"
/**
 * _is - check if a string is lower and returns it'
 * @c : the character to check
 * Return: 1 is c is lower else 0
*/
int _isdigit(int c)
{
if (c < 0 || c > 9)
return (0);
else
return (1);
}







