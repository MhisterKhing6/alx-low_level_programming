#include <stdio.h>
#include "main.h"

/**
 * print_most_numbers- print alphabet in lower case'
 * return 0
 */
void print_most_numbers(void)
{
int start = 0;
while (start <= 9)
{
if (!(start == 2 || start == 4)
_putchar(star + '0');
start++;
}
_putchar('\n');
}

