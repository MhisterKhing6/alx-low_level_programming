#include <stdlib.h>
#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _string_toupper - print strings to the console
 * @dest: a holder to contained the copied file
 * Return: returns a pointer to the copied folder
 */
char  *_string_toupper(char *dest)
{
return (strupr(dest));
}
