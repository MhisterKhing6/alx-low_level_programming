#include <stdio.h>
#include "main.h"
#include <string.h>
#include <stdlib.h>
/**
 * _strcmp - print strings to the console
 * @dest: a holder to contained the copied file
 * @src: a holder to copy string from
 * Return: an integer representing the if equal or not
 */
int  _strcmp(char *dest, char *src)
{
return (strcmp(dest, src));
}
