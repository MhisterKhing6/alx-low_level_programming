#include <stdio.h>
#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * print_number - print strings to the console
 * @i: a holder to copy string from
 * Return: returns a pointer to the copied folder
 */
void print_number(int i)
{
printf("%d", i);
}
